import styled from 'styled-components';
import tw from 'twin.macro';

const Input = styled.input`
  ${tw`w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500`}
`;

export default Input;
